name: Sincronizar Main com Branches de Clientes

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [closed]

jobs:
  sync-branches:
    # Só executa quando um PR é mergeado ou em push direto na main
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Busca todo o histórico
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Configurar Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Sincronizar com branches de clientes
        env:
          # Formato esperado: "cliente-a cliente-b cliente-c"
          CLIENT_BRANCHES: ${{ vars.CLIENT_BRANCHES }}
        run: |
          # Garante que estamos na main primeiro
          git checkout main
          git pull origin main
          
          for branch in $CLIENT_BRANCHES; do
            echo "🔄 Sincronizando branch: $branch"
            
            # Verifica se a branch existe remotamente
            if git ls-remote --heads origin $branch | grep -q $branch; then
              git checkout $branch
              git pull origin $branch
              
              # Tenta fazer merge da main
              if git merge origin/main --no-edit; then
                echo "✅ Merge bem-sucedido na branch $branch"
                git push origin $branch
              else
                echo "⚠️ Conflito detectado na branch $branch"
                echo "Abortando merge..."
                git merge --abort
                
                # Cria issue sobre o conflito
                gh issue create \
                  --title "⚠️ Conflito ao sincronizar branch: $branch" \
                  --body "Conflito detectado ao sincronizar main com $branch. Resolução manual necessária."
              fi
            else
              echo "⚠️ Branch $branch não existe, pulando..."
            fi
            
            # Volta para main antes da próxima iteração
            git checkout main
          done
      
      - name: Notificar resultado
        if: always()
        run: |
          echo "🏁 Sincronização concluída!"
          echo "Verifique os logs acima para detalhes de cada branch."
